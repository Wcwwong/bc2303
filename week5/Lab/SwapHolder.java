package week5.Lab;

import java.util.Random;

public class SwapHolder implements Swap<SwapHolder> {
String
Lab.swap(strings, ss);
  @Override
  public int indexForSwap(int listSize){
    return new Random().nextInt();
  }
  
}
