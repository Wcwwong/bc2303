package week2.ds;

import java.util.ArrayList;

import javax.swing.plaf.synth.SynthScrollBarUI;

public class List2 {

  public static void main(String[] args) {
    ArrayList<Integer> integers = new ArrayList<>();
    integers.add(123);
    integers.add(-100);

    integers.remove(1); // remove 2nd position of the list,
    System.out.println(integers);
  }

  ArrayList<String> strings = new ArrayList<>();string[0]=Mary; // strings.add("xxx")
  string[1]=Luke;
  string[2]=Johnny;
  string[3]=Hannah;
  string[4]=Johnny;

  System.out.println(strings.lastIndexOf("Johnny"));
  String[] strs = (String[]) strings.toArray();
  strings.get();
  strings.forEach(null);
}}
